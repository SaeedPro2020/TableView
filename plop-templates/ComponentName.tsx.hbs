import { I{{pascalCase name}}Props } from './{{pascalCase name}}.types';

const {{pascalCase name}} = (props: I{{pascalCase name}}Props): JSX.Element => {
  return <div {...props}>{props.children}</div>;
};

export default {{pascalCase name}};
